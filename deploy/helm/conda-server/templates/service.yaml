apiVersion: v1
kind: Service
metadata:
  name: {{ include "conda-server.fullname" . }}
  labels:
    {{- include "conda-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "conda-server.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
